fastapi==0.135.3
uvicorn==0.44.0
pydantic-settings==2.13.1
python-dotenv==1.2.2
pyjwt[crypto]==2.10.1
# Use slim provider extras only; the full pydantic-ai meta-package pulls all providers
# and Render currently fails resolving the mistral extra (`mistralai>=2.0.0`).
pydantic-ai-slim[anthropic,google,openai]==1.80.0
# Benchmark queue persistence for hosted queued/cron benchmark mode.
psycopg[binary]==3.3.2
# Pinned to explicit Core-Memory commit for deploy stability.
# PR #186 branch dependency (not merged yet): exposes adaptive causal trace depth
# through canonical retrieval, public memory trace, and the PydanticAI trace tool;
# default official trace depth is 6 and diagnostics report requested/effective caps.
# Keep this SHA until the end-to-end benchmark-production-fidelity PRD is complete,
# then update to the merged Core-Memory commit before final release if needed.
core-memory[qdrant,kuzu,mcp] @ git+https://github.com/JohnnyFiv3r/Core-Memory.git@b88e93e9c1b49a8f6abbc3442175b0f1d43696fa
# Keep Qdrant explicit so deployment logs fail loudly if the semantic backend
# dependency is skipped instead of silently falling back to lexical mode.
qdrant-client[fastembed]>=1.9
